pipeline {
    agent any // This means Jenkins can run the pipeline on any available agent (the EC2 instance itself, in this case)

    stages {
        stage('Hello') {
            steps {
                echo 'Hello, Jenkins! This is my first pipeline build.'
            }
        }
    }
}
